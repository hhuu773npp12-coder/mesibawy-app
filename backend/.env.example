# Backend environment variables for Mesibawy
# Copy this file to .env and adjust values

# --- Server ---
PORT=3000
# Use production on the VPS
NODE_ENV=production

# --- PostgreSQL ---
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=mesibawy

# --- Auth/JWT ---
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# --- CORS / WebSocket ---
# Comma-separated origins. Example for production domain(s):
WS_CORS_ORIGIN=https://mesibawy.com,https://www.mesibawy.com

# --- Notifications (OneSignal) ---
ONESIGNAL_APP_ID=
ONESIGNAL_API_KEY=
